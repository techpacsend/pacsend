import { FormatdatesPipe } from './formatdates.pipe';

describe('FormatdatesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatdatesPipe();
    expect(pipe).toBeTruthy();
  });
});
